.venv/
venv/
__pycache__/
*.pyc
.DS_Store

# Stav řetězu je odvozený z modelu — patří k běhu, ne do historie.
*.state.json

# Zdrojové dokumenty se neverzují — jsou to cizí texty, ne naše práce.
# Výřez AI Actu si vyrobí `python3 utils/build-vyrez.py` z EUR-Lexu.
inputs/

# Projekce se neverzují — regenerují se. Je to celá pointa tohohle repozitáře.
# `bash build.sh <model.yaml>` je vyrobí znovu za pár sekund.
out/

# Modely z běhů jsou lokální. Do historie patří jen model, který někdo prošel
# a chce ho ukázat jako příklad — ten se přidá přes `git add -f`.
model/
